# Albert Websearch Plugin (Extended Placeholders)

## Placeholders

Supported in engine URLs:

- `%s` -> full query
- `%1 %2 %3 ...` -> positional arguments

Arguments are split by spaces.

Quoted arguments are supported
